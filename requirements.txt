# 山海经异兽自走棋 - 依赖包列表
# 基础依赖，Python标准库已包含大部分功能

# 如果需要额外的功能扩展，可以添加以下包：
# colorama>=0.4.4          # 跨平台彩色输出支持
# rich>=10.0.0             # 富文本和美化输出
# click>=8.0.0             # 命令行界面增强

# 当前版本使用Python标准库，无需额外安装
# 确保Python版本 >= 3.7
